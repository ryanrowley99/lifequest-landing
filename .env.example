# LifeQuest landing — reference only.
# These values are hardcoded in script.js (anon key is public-safe with INSERT/UPDATE-only RLS).
# Never commit a service_role key anywhere in this repo.

SUPABASE_URL=https://[project-ref].supabase.co
SUPABASE_ANON_KEY=[anon-public-key]
STRIPE_PAYMENT_LINK=https://buy.stripe.com/[link-id]
